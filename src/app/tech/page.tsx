import type { Metadata } from "next";
import TechSection from "@/components/TechSection";

export const metadata: Metadata = {
  title: "Tech — Eden Huang",
};

export default function TechPage() {
  return <TechSection />;
}
