import type { Metadata } from "next";
import InsightsSection from "@/components/InsightsSection";

export const metadata: Metadata = {
  title: "Insights — Eden Huang",
};

export default function InsightsPage() {
  return <InsightsSection />;
}
