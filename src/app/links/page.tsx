import type { Metadata } from "next";
import LinksSection from "@/components/LinksSection";

export const metadata: Metadata = {
  title: "Links — Eden Huang",
};

export default function LinksPage() {
  return <LinksSection />;
}
